apiVersion: v1
kind: Service
metadata:
  name: gpt-api-service
spec:
  type: LoadBalancer
  selector:
    app: gpt-api
  ports:
    - protocol: TCP
      port: 80         # Public port
      targetPort: 8000 # Your FastAPI app inside container
